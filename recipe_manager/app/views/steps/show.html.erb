<p style="color: green"><%= notice %></p>

<%= render @step %>

<div>
  <%= link_to "Edit this step", edit_step_path(@step) %> |
  <%= link_to "Back to recipe", recipe_path(@step.recipe) %>

  <%= button_to "Delete this step", @step, method: :delete %>
</div>
