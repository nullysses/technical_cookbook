<%= form_with(model: adjustment) do |form| %>
  <% if adjustment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(adjustment.errors.count, "error") %> prohibited this adjustment from being saved:</h2>

      <ul>
        <% adjustment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :recipe_id, style: "display: block" %>
    <%= form.text_field :recipe_id %>
  </div>

  <div>
    <%= form.label :condition, style: "display: block" %>
    <%= form.textarea :condition %>
  </div>

  <div>
    <%= form.label :fix, style: "display: block" %>
    <%= form.textarea :fix %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
