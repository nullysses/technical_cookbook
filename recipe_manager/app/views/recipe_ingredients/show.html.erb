<p style="color: green"><%= notice %></p>

<%= render @recipe_ingredient %>

<div class="page-actions">
  <%= link_to "Edit recipe ingredient", edit_recipe_ingredient_path(@recipe_ingredient), class: "button" %>
  <%= link_to "Back to recipe ingredients", recipe_ingredients_path, class: "button" %>

  <%= button_to "Delete recipe ingredient", @recipe_ingredient, method: :delete, class: "button danger" %>
</div>
