<p style="color: green"><%= notice %></p>

<% content_for :title, "Substitutes" %>

<h1>Substitutes</h1>

<div id="substitutes">
  <% @substitutes.each do |substitute| %>
    <%= render substitute %>
    <p>
      <%= link_to "Show this substitute", substitute %>
    </p>
  <% end %>
</div>

<%= link_to "New substitute", new_substitute_path %>
